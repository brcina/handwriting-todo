server:
  servlet:
    context-path: /api

spring:
  application:
    name: handwriting-todo-be
  ai:
    ollama:
      base-url: "${SPRING_AI_OLLAMA_BASE_URL:http://localhost:11434}"
      chat:
        options:
          model: llama3.2-vision:11b
          temperature: 0.7
